import { Request, Response } from "express";

export class CreateContractorUploadController {
    async handle(request: Request, response: Response) {
        const { id } = request.params;
        
    }
}