
import { prisma } from "../../../../database/prismaClient";
import { AppError } from "../../../../middlewares/AppError";
export class GetClientsContractorByIdUseCase {
    async execute(/*{ id: number }*/) {
        // return prisma.clientsContractors.findMany({
        //     where: {

        //     },
        // });
    }
}